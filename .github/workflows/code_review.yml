name: Code review

on: [pull_request]

jobs:
  run_script:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          openai-key: ${{ secrets.OPENAI_API_KEY }}
